package com.cos.demo.scene;

import java.util.concurrent.ConcurrentHashMap;

public class SceneManager {
//    private ConcurrentHashMap<String,SceneContext> sceneMap =
//            new ConcurrentHashMap<>();
//
//    public SceneContext get(String sessionId) {
//        return sceneMap.computeIfAbsent(sessionId, k -> new SceneContext());
//    }
//
//    public void enter(String sessionId,Scene scene) {
//        get(sessionId).setScene(scene);
//    }
//
//    public void exit(String sessionId) {
//        get(sessionId).setScene(Scene.NORMAL);
//    }
}
